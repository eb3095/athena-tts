apiVersion: v1
kind: Service
metadata:
  name: {{ include "athena-tts.fullname" . }}
  labels:
    {{- include "athena-tts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "athena-tts.selectorLabels" . | nindent 4 }}
